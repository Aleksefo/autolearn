import { State } from './types'

export const initialState: State = {
  savedTermList: [],
  sourceLanguage: 'es',
  targetLanguage: 'en',
  // counterStatus: 'stopped',
  // setsTime: [60, 60, 60, 60],
  // totalRounds: 2,
  // currentRound: 1,
  // totalSets: 3,
  // currentSet: 1,
  // timeSession: 60,
  // timeSessionLeft: 60,
  // totalTimeLeft: 360,
  stateLoaded: false,
  // volumeState: 'on',
  // themeState: 'light',
  // theme: themes.light,
}
